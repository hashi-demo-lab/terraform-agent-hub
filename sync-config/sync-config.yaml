# terraform-agent-hub sync configuration
# Controls how files are synced from this hub to downstream repos.
# See sync-config/README.md for full schema documentation.

version: "1"

# ──────────────────────────────────────────────
# Defaults applied to all syncs unless overridden
# ──────────────────────────────────────────────
defaults:
  target_branch: main
  pr_labels:
    - hub-sync
  branch_prefix: "hub-sync/"
  commit_prefix: "chore(sync):"
  sync_deletions: true
  update_existing_pr: true

# ──────────────────────────────────────────────
# Patterns excluded from ALL syncs globally
# ──────────────────────────────────────────────
global_excludes:
  - ".DS_Store"
  - ".gitkeep"
  - "**/README.md"
  - "**/.terraform*"
  - "**/*.tfstate*"

# ──────────────────────────────────────────────
# Profiles — groups of repos sharing sync mappings
# ──────────────────────────────────────────────
profiles:
  # Standard profile: full sync of all content directories
  standard:
    repos:
      - hashi-demo-lab/ai-iac-module-template
    mappings:
      - source: agents/
        dest: .claude/agents/
      - source: skills/
        dest: .claude/skills/
      - source: scripts/
        dest: .foundations/scripts/bash
      - source: templates/
        dest: .foundations/templates/

  # GitHub Enterprise profile: same mappings, different host
  ghe-standard:
    host: "github.ibm.com"
    repos:
      - AdvArch/ai-iac-module-template
    mappings:
      - source: agents/
        dest: .claude/agents/
      - source: skills/
        dest: .claude/skills/
      - source: scripts/
        dest: .foundations/scripts/bash
      - source: templates/
        dest: .foundations/templates/

# ──────────────────────────────────────────────
# Standalone repos — custom one-off mappings
# ──────────────────────────────────────────────
# standalone:
#   hashi-demo-lab/special-repo:
#     mappings:
#       - source: agents/
#         dest: .github/claude/agents/
#       - source: scripts/
#         dest: .ci/scripts/
